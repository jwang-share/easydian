can.Control('Apps.UserCtrl', {
        pluginName: 'UserCtrl',
        defaults: {
            current_user: null
        }
    },
    {
        init: function(element) {
            var self = this;
        }
});